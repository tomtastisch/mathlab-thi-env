[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thi-ki-suite"
version = "0.1.0"
description = "THI KI modular workspace"
authors = [{ name = "Tom Werner" }]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["thi"]